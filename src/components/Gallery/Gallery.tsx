import './Gallery.scss'

function Gallery() {
  return (
    <section id={'galleria'} data-scroll={'galleria'} className={'gallery-container'}>
      Gallery
    </section>
  )
}

export default Gallery
